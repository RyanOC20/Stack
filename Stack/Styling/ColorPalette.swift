import SwiftUI

enum ColorPalette {
    static let background = Color(red: 30 / 255, green: 30 / 255, blue: 30 / 255)
    static let rowHover = Color(red: 36 / 255, green: 36 / 255, blue: 36 / 255)
    static let rowSelection = Color(red: 45 / 255, green: 45 / 255, blue: 45 / 255)
    static let textPrimary = Color("TextPrimary")
    static let textSecondary = Color("TextSecondary")
    static let pillBackground = Color.white.opacity(0.08)
}
